<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.poc</groupId>
        <artifactId>task-management-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>task-management</artifactId>
    <packaging>jar</packaging>

    <name>Task Management PoC — Backend</name>
    <description>
        CQRS / Event Sourcing PoC using Axon Framework + Quarkus.
        Validates the Axon approach versus CRUD for a task management use case.
    </description>

    <properties>
        <quarkus.platform.version>3.32.4</quarkus.platform.version>
        <axon.version>5.3.1</axon.version>
        <lombok.version>1.18.32</lombok.version>
        <surefire.plugin.version>3.2.5</surefire.plugin.version>
        <openapi.generator.version>7.8.0</openapi.generator.version>
        <jacoco.plugin.version>0.8.12</jacoco.plugin.version>
        <assertj.version>3.26.3</assertj.version>
        <!-- Where SonarQube picks up the merged JaCoCo report (default location,
             stated explicitly for clarity). -->
        <sonar.coverage.jacoco.xmlReportPaths>${project.build.directory}/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>
    </properties>

    <!-- ===================================================================
         Dependency Management — import Quarkus BOM so all Quarkus artefacts
         can be declared without explicit versions.
    =================================================================== -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!--
                Axon 5's event serialization uses Jackson 3 (tools.jackson.*),
                which depends on the Jackson 2.x annotations module at version
                2.22 (it adds classes such as JsonApplyView).  The Quarkus BOM
                pins jackson-annotations to 2.21, which would be selected by
                dependency mediation and cause a NoClassDefFoundError at runtime.
                Override it to 2.22 (backward compatible with Quarkus's Jackson 2
                databind, as the 2.x annotations line is additive).
            -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>2.22</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!-- ===============================================================
             Quarkus — core runtime
        =============================================================== -->

        <!-- JAX-RS reactive + Jackson JSON serialisation -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>

        <!-- JPA (Hibernate ORM) with Panache active-record helpers -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm-panache</artifactId>
        </dependency>

        <!-- H2 in-memory JDBC driver (event store, read model, saga store) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-h2</artifactId>
        </dependency>

        <!--
            MSSQL JDBC driver — uncomment when switching to SQL Server.
            Also update application.properties:
              - quarkus.datasource.db-kind=mssql
              - quarkus.datasource.jdbc.url=jdbc:sqlserver://...
              - axon.event-store.sql-error-codes.database-product-name=Microsoft SQL Server
        -->
        <!--
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-mssql</artifactId>
        </dependency>
        -->

        <!-- CDI / ArC -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>

        <!-- Validation (Bean Validation 3 / Hibernate Validator) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-validator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-micrometer</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-opentelemetry</artifactId>
        </dependency>

        <!-- ===============================================================
             Axon Framework 5 — CQRS / Event Sourcing kernel
             Module structure changed in Axon 5 (axon-configuration was folded
             into the individual modules; each configurer lives in its module):
               axon-messaging     — commands, events, queries, MessagingConfigurer
               axon-modelling     — event-sourced entities, ModellingConfigurer
               axon-eventsourcing — JPA event store, EventSourcingConfigurer
               axon-conversion    — Converter / JacksonConverter (Jackson 3)

             NOTE: Axon 5.3.1 has no saga/deadline/scheduling modules; those
             flows are implemented natively (see saga package process managers).
        =============================================================== -->
        <dependency>
            <groupId>org.axonframework</groupId>
            <artifactId>axon-messaging</artifactId>
            <version>${axon.version}</version>
        </dependency>
        <dependency>
            <groupId>org.axonframework</groupId>
            <artifactId>axon-modelling</artifactId>
            <version>${axon.version}</version>
        </dependency>
        <dependency>
            <groupId>org.axonframework</groupId>
            <artifactId>axon-eventsourcing</artifactId>
            <version>${axon.version}</version>
        </dependency>
        <dependency>
            <groupId>org.axonframework</groupId>
            <artifactId>axon-conversion</artifactId>
            <version>${axon.version}</version>
        </dependency>

        <!-- ===============================================================
             Lombok — compile-time code generation (logger, constructor, etc.)
             optional=true: excluded from the packaged jar / transitive deps.
        =============================================================== -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.swagger</groupId>
            <artifactId>swagger-annotations</artifactId>
            <version>1.6.14</version>
        </dependency>

        <!-- ===============================================================
             Test scope
        =============================================================== -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Supplies the JaCoCo coverage agent in a way that understands the
             Quarkus test classloader; the jacoco-maven-plugin renders the report
             from the execution data it writes. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jacoco</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Axon 5 test fixtures: AxonTestFixture -->
        <dependency>
            <groupId>org.axonframework</groupId>
            <artifactId>axon-test</artifactId>
            <version>${axon.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>1.3.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Quarkus build plugin — handles dev mode, native compile, etc. -->
            <plugin>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                    <!-- Enable parameter name retention so Jackson can
                         deserialise records without explicit @JsonProperty -->
                    <parameters>true</parameters>
                    <!--
                      Explicitly list annotation processors so that Lombok runs
                      during javac before Quarkus's build-time CDI processing.
                      Without this, auto-detected processors from the classpath
                      may run in an unpredictable order.
                    -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.plugin.version}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>
                            org.jboss.logmanager.LogManager
                        </java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                        <!-- Point the quarkus-jacoco extension at the same exec
                             file the jacoco-maven-plugin agent appends to, and
                             stop it from wiping that file, so @QuarkusTest and
                             plain unit-test coverage merge into one report. -->
                        <quarkus.jacoco.data-file>${project.build.directory}/jacoco-quarkus.exec</quarkus.jacoco.data-file>
                        <quarkus.jacoco.reuse-data-file>true</quarkus.jacoco.reuse-data-file>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <!-- JaCoCo — code coverage across BOTH test kinds.

                 @QuarkusTest loads application classes through the Quarkus
                 runtime classloader; plain unit tests (TaskAggregate, sagas via
                 Axon fixtures) load them through the normal classloader.  To
                 count both in one report we run two coverage sources that write
                 to the SAME exec file (target/jacoco-quarkus.exec):

                   * the quarkus-jacoco extension (test dependency) records the
                     @QuarkusTest classloader.  quarkus.jacoco.reuse-data-file
                     (set in the surefire config) stops it from wiping the file.
                   * this plugin's prepare-agent records everything else.
                     exclClassLoaders=*QuarkusClassLoader keeps it from also
                     instrumenting the Quarkus-loaded classes (which would fail
                     with "already instrumented"); append=true adds to the file.

                 The report goal then renders the combined data into
                 target/site/jacoco (jacoco.xml is consumed by Sonar via
                 sonar.coverage.jacoco.xmlReportPaths). -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.plugin.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco-quarkus.exec</destFile>
                            <append>true</append>
                            <exclClassLoaders>*QuarkusClassLoader</exclClassLoaders>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-quarkus.exec</dataFile>
                            <excludes>
                                <!-- OpenAPI-generated sources are not our code to cover -->
                                <exclude>eu/poc/taskmanagement/generated/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <version>${openapi.generator.version}</version>
                <executions>
                    <execution>
                        <id>generate-http-api</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <generatorName>jaxrs-spec</generatorName>
                            <inputSpec>${project.basedir}/src/main/openapi/task-management-api.yaml</inputSpec>
                            <output>${project.build.directory}/generated-sources/openapi</output>
                            <apiPackage>eu.poc.taskmanagement.generated.api</apiPackage>
                            <modelPackage>eu.poc.taskmanagement.generated.model</modelPackage>
                            <invokerPackage>eu.poc.taskmanagement.generated.invoker</invokerPackage>
                            <addCompileSourceRoot>true</addCompileSourceRoot>
                            <generateApiTests>false</generateApiTests>
                            <generateModelTests>false</generateModelTests>
                            <generateApiDocumentation>false</generateApiDocumentation>
                            <generateModelDocumentation>false</generateModelDocumentation>
                            <configOptions>
                                <interfaceOnly>true</interfaceOnly>
                                <useTags>true</useTags>
                                <useJakartaEe>true</useJakartaEe>
                                <dateLibrary>java8</dateLibrary>
                                <performBeanValidation>true</performBeanValidation>
                                <useBeanValidation>true</useBeanValidation>
                                <openApiNullable>false</openApiNullable>
                            </configOptions>
                            <additionalProperties>
                                <hideGenerationTimestamp>true</hideGenerationTimestamp>
                            </additionalProperties>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>add-openapi-generated-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.build.directory}/generated-sources/openapi/src/gen/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
